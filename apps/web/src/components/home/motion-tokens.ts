/**
 * A curva da casa (Regra da Suavização em DESIGN.md): o mesmo ease-out do
 * CSS (`--ease-brand` no @theme) em forma de array para o motion/react.
 * Mudou aqui, muda lá.
 */
export const BRAND_EASE = [0.16, 1, 0.3, 1] as const;
