import { motionValue } from "motion/react";

/**
 * Progresso único da jornada (0 = bolha do hero, 1 = doca), publicado pela
 * caixa viajante e consumido pelo caminho líquido — os dois andam juntos.
 */
export const journeyProgress = motionValue(0);
